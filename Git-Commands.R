terminal/command line commands:
  pwd, cd, ls, touch, :q

git commands:
  git init
  git status
  git add = to stage
  git commit -m "your commit message inputted here" = committing
  git push
  git touch  
  git diff
  